<?php
session_start();
include 'includes/header.php';
include 'includes/db.php';
include_once 'includes/auth.php';

if (isset($_POST['login'])) {
    $email = trim($_POST['email']);
    $password = $_POST['password'];

    $sql = "SELECT * FROM users WHERE email = ?";
    $stmt = $pdo->prepare($sql);
    $stmt->execute([$email]);
    $user = $stmt->fetch();

    if ($user && password_verify($password, $user['password_hash'])) {
        $_SESSION['user_id'] = $user['id'];
        $_SESSION['role'] = $user['role'];
        $_SESSION['name'] = $user['name'];

        echo "<p style='color:green;'>Prisijungimas sėkmingas!</p>";

        // 🔽 Tikslus nukreipimas pagal rolę
        if ($user['role'] === 'admin' || $user['role'] === 'manager') {
            echo "<meta http-equiv='refresh' content='1;url=manager/reservations.php'>";
        } else {
            echo "<meta http-equiv='refresh' content='1;url=index.php'>";
        }

    } else {
        echo "<p style='color:red;'>Neteisingas el. paštas arba slaptažodis.</p>";
    }
}
?>

<h2>Prisijungimas</h2>
<form method="post">
  <label>El. paštas:</label><br>
  <input type="email" name="email" required><br><br>
  <label>Slaptažodis:</label><br>
  <input type="password" name="password" required><br><br>
  <button type="submit" name="login">Prisijungti</button>
</form>

<p>Neturite paskyros? <a href="register.php">Registruokitės</a></p>

<?php include 'includes/footer.php'; ?>

